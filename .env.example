# Same variable — pick one value per environment

# Local backend (npm run dev + uvicorn on :8000)
# VITE_API_URL=http://localhost:8000

# Hugging Face backend (default — works without local server)
VITE_API_URL=https://aliashrafabbasi-ai-resume-screener.hf.space

# Production build uses .env.production (same HF URL)
